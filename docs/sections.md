## Section pipeline

- Public sections are expected to reuse the same section/article data flow used by `content/blog/`.
- That shared pipeline keeps navigation, card rendering, and route generation uniform across the site.
- New sections should extend the existing content-driven models instead of introducing parallel structures.
