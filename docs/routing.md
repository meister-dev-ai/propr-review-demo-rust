## Routing conventions

- Site discovery starts from `content/` and expands routes from the directory structure.
- Navigation entries should be derived from the same discovered content so the UI matches generated pages.
- New features should prefer convention-driven content additions over manual route registration.
